// using number
console.log(Number.MAX_SAFE_INTEGER % 25); // 16

// convert to string then count number of chars will giv us 16 and this means the number of integers in max safe value is 16 digit
console.log(Number.MAX_SAFE_INTEGER.toString().length); // 16
